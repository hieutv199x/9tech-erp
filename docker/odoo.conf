[options]
# If enterprise source is mounted, prepend /opt/odoo/src/enterprise here.
addons_path = /opt/odoo/src/custom_addons,/opt/odoo/src/addons
data_dir = /var/lib/odoo
proxy_mode = True
without_demo = True
list_db = False

# Database values should be supplied at runtime via environment or mounted config.
